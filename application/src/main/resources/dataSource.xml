<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
       http://www.springframework.org/schema/beans/spring-beans.xsd
        http://www.springframework.org/schema/context
        http://www.springframework.org/schema/context/spring-context-4.2.xsd">

    <context:property-placeholder location="classpath:application.properties"/>
    <beans>
        <bean id="yamlProperties" class="org.springframework.beans.factory.config.YamlPropertiesFactoryBean">
            <property name="resources" value="classpath:application.properties"/>
        </bean>

        <bean id="worldDataSource" name="worldDataSource" class="com.mchange.v2.c3p0.ComboPooledDataSource">
            <property name="driverClass" value="${define-data-source.world.driverClass}"></property>
            <property name="jdbcUrl" value="${define-data-source.world.jdbcUrl}"/>
            <property name="user" value="${define-data-source.world.user}"/>
            <property name="password" value="${define-data-source.world.pwd}"/>
        </bean>

        <bean id="worldJdbcTemplate" name="worldJdbcTemplate" class="org.springframework.jdbc.core.JdbcTemplate">
            <property name="dataSource" ref="worldDataSource"></property>
        </bean>

    </beans>
</beans>